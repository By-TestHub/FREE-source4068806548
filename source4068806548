local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local player = Players.LocalPlayer

local infiniteJumpActive = false
UIS.JumpRequest:Connect(function()
    if infiniteJumpActive then
        local humanoid = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end
end)

local noclipActive = false
RunService.Stepped:Connect(function()
    if noclipActive and player.Character then
        for _, part in pairs(player.Character:GetDescendants()) do
            if part:IsA("BasePart") and part.CanCollide == true then
                part.CanCollide = false
            end
        end
    end
end)

local flying = false
local speed = 80 
local bv, bg

local function toggleFly(state)
    flying = state
    local character = player.Character
    if not character then return end
    
    local root = character:FindFirstChild("HumanoidRootPart")
    local hum = character:FindFirstChild("Humanoid")
    
    if flying and root and hum then
        bv = Instance.new("BodyVelocity", root)
        bv.MaxForce = Vector3.new(1e9, 1e9, 1e9)
        bv.Velocity = Vector3.zero
        
        bg = Instance.new("BodyGyro", root)
        bg.MaxTorque = Vector3.new(1e9, 1e9, 1e9)
        bg.P = 10000
        bg.D = 100
        bg.CFrame = root.CFrame
        
        hum.PlatformStand = true
    else
        if bv then bv:Destroy() end
        if bg then bg:Destroy() end
        if hum then hum.PlatformStand = false end
    end
end

RunService.RenderStepped:Connect(function()
    if flying and player.Character then
        local root = player.Character:FindFirstChild("HumanoidRootPart")
        local hum = player.Character:FindFirstChild("Humanoid")
        local cam = workspace.CurrentCamera
        
        if root and hum and bv and bg then
            local moveDir = hum.MoveDirection
            if moveDir.Magnitude > 0 then
                local camLook = cam.CFrame.LookVector
                local flatLook = Vector3.new(camLook.X, 0, camLook.Z).Unit
                local camRight = cam.CFrame.RightVector
                local flatRight = Vector3.new(camRight.X, 0, camRight.Z).Unit
                
                local forwardIntent = moveDir:Dot(flatLook)
                local rightIntent = moveDir:Dot(flatRight)
                local flyDir = (cam.CFrame.LookVector * forwardIntent) + (cam.CFrame.RightVector * rightIntent)
                bv.Velocity = flyDir * speed 
            else
                bv.Velocity = Vector3.zero
            end
            bg.CFrame = cam.CFrame
        end
    end
end)

local flingActive = false
local spinSpeed = 5000
RunService.Heartbeat:Connect(function()
    if flingActive and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        local root = player.Character.HumanoidRootPart
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        local oldRotVel = root.RotVelocity
        root.RotVelocity = Vector3.new(0, spinSpeed, 0)
        
        RunService.RenderStepped:Wait()
        root.RotVelocity = oldRotVel
        if hum then
            if hum:GetState() == Enum.HumanoidStateType.FallingDown or hum:GetState() == Enum.HumanoidStateType.Ragdoll then
                hum:ChangeState(Enum.HumanoidStateType.GettingUp)
            end
        end
    end
end)

local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
local Window = WindUI:CreateWindow({
    Title = "TEST HUB | รวมหลายฟังก์ชันฟรี",
    Icon = "rbxassetid://117649776522765",
    Author = "TEST HUB",
    Folder = "TESTHUB_MM2",
    Size = UDim2.fromOffset(550, 420),
    Theme = "Dark",
    Transparent = true,
    Resizable = true,
})

local Tab1 = Window:Tab({ Title = "ผู้เล่น", Icon = "user" })
local Tab2 = Window:Tab({ Title = "บิน", Icon = "plane" })
local Tab3 = Window:Tab({ Title = "เกรียน", Icon = "zap" })
local Tab4 = Window:Tab({ Title = "อธิบาย + ตั้งค่า", Icon = "info" })

Tab1:Input({
    Title = "ปรับค่าวิ่งไว",
    Value = "",
    Placeholder = "ใส่ความเร็ว...",
    Type = "Input",
    Callback = function(txt)
        local speed = tonumber(txt) 
        if speed then
            player.Character.Humanoid.WalkSpeed = speed
        end
    end
})

Tab1:Button({
    Title = "รีเซ็ตค่าวิ่งเร็วให้เหลือ 16",
    Callback = function()
        player.Character.Humanoid.WalkSpeed = 16
    end
})

Tab1:Input({
    Title = "ปรับค่ากระโดดสูง",
    Value = "",
    Placeholder = "ใส่ค่ากระโดด...",
    Type = "Input",
    Callback = function(txt)
        local jump = tonumber(txt)
        if jump then
            local hum = player.Character.Humanoid
            hum.UseJumpPower = true
            hum.JumpPower = jump
        end
    end
})

Tab1:Button({
    Title = "รีเซ็ตค่ากระโดดสูงให้เหลือ 50",
    Callback = function()
        player.Character.Humanoid.JumpPower = 50
    end
})

Tab1:Toggle({
    Title = "กระโดดได้ไม่จำกัด",
    Value = false,
    Callback = function(state)
        infiniteJumpActive = state
    end
})

Tab1:Toggle({
    Title = "เดินทะลุกำแพง",
    Value = false,
    Callback = function(state)
        noclipActive = state
        if not state and player.Character then
            for _, part in pairs(player.Character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = true
                end
            end
        end
    end
})

Tab2:Button({
    Title = "บินแบบ FLY GUI V3 ไม่ทะลุกำแพง",
    Callback = function()
        loadstring(game:HttpGet("https://pastebin.com/raw/2RiADZZu"))()
    end
})

Tab2:Button({
    Title = "บินแบบ FLY GUI V4 ไม่ทะลุกำแพง",
    Callback = function()
        loadstring(game:HttpGet("https://pastebin.com/raw/n6Q5TNyg"))()
    end
})

Tab2:Toggle({
    Title = "บินแบบปกติไม่ทะลุกำแพง",
    Value = false,
    Callback = function(state)
        toggleFly(state)
    end
})

Tab2:Input({
    Title = "ปรับความเร็วบินแบบปกติ",
    Value = tostring(speed),
    Placeholder = "ใส่ความเร็วบิน...",
    Type = "Input",
    Callback = function(txt)
        local s = tonumber(txt)
        if s then speed = s end
    end
})

Tab3:Button({
    Title = "เสกของ",
    Callback = function()
        loadstring(game:HttpGet("https://pastebin.com/raw/WPpPKCHE"))()
    end
})

Tab3:Button({
    Title = "ชักว่าว R6",
    Callback = function()
        loadstring(game:HttpGet("https://pastefy.app/3q0ZUYSV/raw"))()
    end
})

Tab3:Toggle({
    Title = "เดินชนกระเด็น",
    Desc = "ใช้ได้สำหรับแมพที่เดินชนกันได้เท่านั้น",
    Value = false,
    Callback = function(state)
        flingActive = state
    end
})

local fpsLabel = Tab4:Paragraph({
    Title = "FPS: กำลังคำนวณ...",
    Desc = "สคริปต์สร้างโดย TEST HUB"
})

Tab4:Button({
    Title = "คัดลอกลิงค์ Discord",
    Desc = "คัดลอกลิงค์กลุ่ม Discord ไว้ในคลิปบอร์ด",
    Callback = function()
        if setclipboard then
            setclipboard("https://discord.gg/Ttwq9Udwhc")
        elseif syn and syn.write_clipboard then
            syn.write_clipboard("https://discord.gg/Ttwq9Udwhc")
        end
        game:GetService("StarterGui"):SetCore("SendNotification", { 
            Title = "คัดลอกสำเร็จ",
            Text = "คัดลอกลิงค์ Discord แล้ว",
            Duration = 5,
        })
    end
})

Tab4:Button({
    Title = "บูส FPS ลดแสงนิด",
    Callback = function()
        local l = game.Lighting
        l.GlobalShadows = true
        l.Brightness = 1
        l.FogEnd = 10000
    end
})

Tab4:Button({
    Title = "บูส FPS ภาพเดิม",
    Callback = function()
        local l = game.Lighting
        l.GlobalShadows = true
        l.Brightness = 2
        l.FogEnd = 100000
    end
})

local lastTime = os.clock()
local frameCount = 0
RunService.RenderStepped:Connect(function()
    frameCount = frameCount + 1
    local currentTime = os.clock()
    if currentTime - lastTime >= 0.3 then
        local fps = math.floor(frameCount / (currentTime - lastTime))
        fpsLabel:SetTitle("FPS: " .. fps)
        lastTime = currentTime
        frameCount = 0
    end
end)

local function createToggleButton(imageAsset)
    if getgenv().OpenGuiButton then getgenv().OpenGuiButton:Destroy() end
    
    local gui = Instance.new("ScreenGui")
    gui.Name = "ToggleGui_Kaprao"
    gui.Parent = (gethui and gethui()) or player:WaitForChild("PlayerGui")
    gui.IgnoreGuiInset = true
    gui.ResetOnSpawn = false
    
    local frame = Instance.new("Frame", gui)
    frame.Size = UDim2.new(0, 35, 0, 35)
    frame.Position = UDim2.new(0, 20, 0, 150)
    frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    frame.BackgroundTransparency = 0.1
    frame.Active = true
    
    local corner = Instance.new("UICorner", frame)
    corner.CornerRadius = UDim.new(0, 8) 
    
    local btn = Instance.new("ImageButton", frame)
    btn.Size = UDim2.new(0, 23, 0, 23)
    btn.Position = UDim2.new(0, 6, 0, 6)
    btn.BackgroundTransparency = 1
    btn.Image = imageAsset
    
    local dragging, dragStart, startPos
    local hasDragged = false
    
    btn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            hasDragged = false
        end
    end)

    UIS.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            if delta.Magnitude > 7 then hasDragged = true end
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    UIS.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)

    btn.Activated:Connect(function()
        if not hasDragged then
            local customToggleUsed = false
            pcall(function()
                if Window and Window.Toggle then
                    Window:Toggle()
                    customToggleUsed = true
                end
            end)
            if not customToggleUsed then
                for _, v in pairs(game:GetService("CoreGui"):GetChildren()) do
                    if v:IsA("ScreenGui") and (v.Name == "WindUI" or v:FindFirstChild("Main") or string.find(v.Name, "Wind")) then
                        v.Enabled = not v.Enabled
                    end
                end
                for _, v in pairs(player:WaitForChild("PlayerGui"):GetChildren()) do
                    if v:IsA("ScreenGui") and (v.Name == "WindUI" or v:FindFirstChild("Main") or string.find(v.Name, "Wind")) then
                        v.Enabled = not v.Enabled
                    end
                end
            end
        end
    end)
    
    getgenv().OpenGuiButton = gui
end

createToggleButton("rbxassetid://117649776522765")
